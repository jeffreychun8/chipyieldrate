import { parameterCategories } from '../constants/parameters.js';

export const generateIndustrialData = () => {
    const data = [];
    const waferRadius = 18;
    const centerX = 20;
    const centerY = 20;
    
    // All data generation logic here
    // ... (moved from main component)
    
    return { headers, data };
};
